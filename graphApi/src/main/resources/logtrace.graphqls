type LogTrace {
    id: String!
    name: String
    requestUrl: String
    requestType: String
    ip: String
    costTime: String
}

type Query {
    logTraces: [LogTrace]
    logTrace(id: String!): LogTrace
}

type Mutation{
    deleteLogTrace(id: String!): Boolean
}